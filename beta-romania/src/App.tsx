import * as React from "react";

export default class App extends React.Component{
    render(): React.ReactNode {
        return(
            <div>
                <h1>Welcome!</h1>
            </div>
        )
    }
}
